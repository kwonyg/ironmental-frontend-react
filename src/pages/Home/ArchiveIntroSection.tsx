import * as React from 'react'
import styled from 'styled-components'

// interface Props {}

const ArchiveIntroSection: React.FC = props => {
  return <Section>아카이브 인트로 섹션</Section>
}

const Section = styled.section``

export default ArchiveIntroSection
